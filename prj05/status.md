\begin{itemize}
\item Loss function was fine and training history was also acceptable as the diffusion model loss for the both training and validation data convereges. Olny the definition/formula was not accurate which I solved it. E means the average of the values in the minibatch.
\item I am currently on medium goals. I have implemented diffusion model as well as the attention based neural operator for the backward prediction. I have also trained visual autoregressive model (VAR) and the the results are not competetive compared to teh diffusion model. The problem is which using VQVAE as the initial step to discretize the latent space and find resolution tokens/codebook to pass to the VAR training for next resolution prediction. However, the problem is probably not with the implementation, but with the concept behind training VAR model. VAR is probably not a suitable model for this problem because of the sdf function that we expect the model to generate. The model does not perform well on the boundaries.
\item working more on the VAR mdoel training and trying to resolve the blurry boundaries. BTW, the diffusion model is already performing well and probably in this problem, it's a better choice for geometry generation using the stress-strain curves.